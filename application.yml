
spring:
  jpa:
    hibernate:
#      ddl-auto: create-drop //update//create
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
